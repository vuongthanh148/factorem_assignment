export const APP_ENV = {
  PROD: 'PROD',
  DEV: 'DEV',
};

export const TOKEN_TYPE = {
  ACCESS: 'ACCESS',
  REFRESH: 'REFRESH',
};

export const STATUS_LIST = {
  PENDING: 'PENDING',
  APPROVED: 'APPROVED',
  ACCEPTED: 'ACCEPTED',
  REJECTED: 'REJECTED',
  DELIVERED: 'DELIVERED',
};

export const DEFAULT_BCRYPT_SALT = 10;
export const DEFAULT_JWT_ACCESS_EXPIRATION = 60;
export const DEFAULT_JWT_REFRESH_EXPIRATION = 30;
export const DEFAULT_PORT = 3000;
export const DEFAULT_PAGE_LIMIT = 10;
export const DEFAULT_PAGE_NUMBER = 1;
export const MIN_PASSWORD_LENGTH = 8;
export const MIN_UPDATE_FIELD = 1;
